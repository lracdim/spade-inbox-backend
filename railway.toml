[build]
builder = "NIXPACKS"
buildCommand = "npm ci --include=dev && node node_modules/typescript/bin/tsc"
 
[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/"
healthcheckTimeout = 30